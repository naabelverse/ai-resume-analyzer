# Windows without Developer Mode cannot create the symlinks that pnpm's default
# node_modules layout relies on (ERR_PNPM_EPERM on symlinkAllModules).
# The hoisted linker copies instead of symlinking.
node-linker=hoisted

# The default 60s fetch timeout aborts partway through a cold install here.
fetch-timeout=300000
